*To update to package*:
Delete and redeploy containers:
```hi && hello```
Use below command on your venv:
```python -m pip install nimble-cfl -U ```
NFS path is already updated with code:
```/auto/lib```
